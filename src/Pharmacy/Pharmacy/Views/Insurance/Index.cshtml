@model List<Pharmacy.Models.Insurances.InsuranceModel>
@{
    ViewBag.Title = "Cateogories";
}

<h2>@ViewData["Title"]</h2>

<div class="row">
    <div class="col-md-12">
        <div class="panel">
            <a class="btn btn-success" asp-controller="Insurance" asp-action="Add">Add new insurance</a>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-md-12">
        <table class="table table-responsive">
            <thead>
                <tr>
                    <th>
                        Name
                    </th>
                    <th>
                        Supported Medicines
                    </th>
                    <th>
                        
                    </th>
                </tr>
            </thead>
            <tbody>
                @if (Model.Any())
                {
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Name</td>
                            <td>@item.SupportedMedicine</td>
                            <td>
                                <a class="btn btn-sm btn-warning" asp-controller="insurance" asp-action="Edit" asp-route-id="@item.Id">Edit</a>
                                <a class="btn btn-sm btn-danger" asp-controller="insurance" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                                <a class="btn btn-sm btn-primary" asp-controller="InsuranceSupport" asp-action="Index" asp-route-id="@item.Id">Supported Medicines</a>

                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="6">No insurance already exist.</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
